
#import <Foundation/Foundation.h>

NSString *StringFromAsterData(Byte *data);        


//: arrowDown
Byte commonPossibleTimer[] = {90, 9, 50, 6, 16, 64, 47, 64, 64, 61, 69, 18, 61, 69, 60, 9};

//: #333333
Byte componentAppealShooSettings[] = {48, 7, 30, 6, 194, 165, 5, 21, 21, 21, 21, 21, 21, 40};

//: arrowUp
Byte spacingAbaseHelper[] = {35, 7, 30, 4, 67, 84, 84, 81, 89, 55, 82, 210};

//: friend_circle_adapter_like
Byte kWallMessage[] = {84, 26, 15, 6, 93, 70, 87, 99, 90, 86, 95, 85, 80, 84, 90, 99, 84, 93, 86, 80, 82, 85, 82, 97, 101, 86, 99, 80, 93, 90, 92, 86, 9};

//: FFFCF7
Byte appLeadingFormat[] = {30, 6, 90, 12, 43, 200, 252, 26, 35, 54, 130, 111, 236, 236, 236, 233, 236, 221, 19};

//: ic_praise%d
Byte componentThingQueryAlert[] = {70, 11, 97, 11, 228, 2, 208, 62, 96, 174, 178, 8, 2, 254, 15, 17, 0, 8, 18, 4, 196, 3, 63};

//: ic_fold
Byte colorOccasionalLogger[] = {77, 7, 55, 7, 158, 20, 105, 50, 44, 40, 47, 56, 53, 45, 95};

// __DEBUG__
// __CLOSE_PRINT__
//
//  JYBubbleMenuView.m
//  JYImageTextCombine
//
//  Created by JackYoung on 2022/4/1.
//  Copyright © 2022 Jack Young. All rights reserved.
//

// __M_A_C_R_O__
//: #import "ToMediatorShore.h"
#import "ToMediatorShore.h"
//: #import "MarkTransformerConductorOutline.h"
#import "MarkTransformerConductorOutline.h"

//: typedef NS_OPTIONS(NSUInteger, ImmenseAfterTrustedUrban) {
typedef NS_OPTIONS(NSUInteger, ImmenseAfterTrustedUrban) {
    //: ImmenseAfterTrustedUrban_Up,
    ImmenseAfterTrustedUrban_Up,
    //: ImmenseAfterTrustedUrban_Down
    ImmenseAfterTrustedUrban_Down
//: };
};

//: @interface ToMediatorShore()
@interface ToMediatorShore()

//: @property (nonatomic, strong)UIImageView *arrowView;
@property (nonatomic, strong)UIImageView *hematocrit;// 气泡的箭头的高度，该高度包含在bubbleHeight里面。
//: @property (nonatomic, assign)ImmenseAfterTrustedUrban directionPriority;
@property (nonatomic, assign)ImmenseAfterTrustedUrban sourceTiming;
//: @property (nonatomic, assign)CGFloat bubbleHeight;
@property (nonatomic, assign)CGFloat sumactJungle;
//: @property (nonatomic, copy)void (^selectBlock)(id data);
@property (nonatomic, copy)void (^proudPresentation)(id data);
//选中文本在window坐标系中的坐标。
//: @property (nonatomic, assign)CGRect selectionTextRectInWindow;
@property (nonatomic, assign)CGRect shear;
//: @property (nonatomic, strong)NSMutableArray *oldContentArray;
@property (nonatomic, strong)NSMutableArray *extended;

//: @property (nonatomic, assign)CGFloat arrowHeight;
@property (nonatomic, assign)CGFloat technology;

//: @property (nonatomic, assign)NSInteger buttonCount;
@property (nonatomic, assign)NSInteger exitQuantity;// 总按钮数量
//: @property (nonatomic, assign)CGFloat bubbleContentMargin;
@property (nonatomic, assign)CGFloat beneathFloat;// 气泡距离内容的最小距离

//: @property (nonatomic, assign)CGFloat bubbleWidth;
@property (nonatomic, assign)CGFloat globuleAspectWidth;

//: @property (nonatomic, strong)UIButton *foldbtn;
@property (nonatomic, strong)UIButton *agnize;//折叠表情按钮
//: @property (nonatomic, assign)CGFloat bubbleBottomMargin;
@property (nonatomic, assign)CGFloat pin;// 气泡距离屏幕上下两边的最小距离
//存放点赞表情的视图
//: @property (nonatomic, assign)CGFloat praiseWidth;
@property (nonatomic, assign)CGFloat redFloat;
//: @property (nonatomic, strong)NSArray *myNewContentArray;
@property (nonatomic, strong)NSArray *array;

//存放button的背景视图
//: @property (nonatomic, strong)UIView *buttonsBgView;
@property (nonatomic, strong)UIView *head;

//指向内容的箭头宽度
//: @property (nonatomic, assign)CGFloat arrowWidth;
@property (nonatomic, assign)CGFloat provisionWidth;

//: @property (nonatomic, assign)CGFloat bubbleLeftMargin;
@property (nonatomic, assign)CGFloat identity;// 气泡距离屏幕左右两边的最小距离
//: @property (nonatomic, copy)void (^praiseBlock)(NSInteger index);
@property (nonatomic, copy)void (^joinPaint)(NSInteger index);

//: @property (nonatomic, strong)UIView *praiseView;
@property (nonatomic, strong)UIView *drawing;

//: @property (nonatomic, assign)BOOL isfold;
@property (nonatomic, assign)BOOL automatic;

//: @end
@end

//: @implementation ToMediatorShore
@implementation ToMediatorShore

//: - (void)onButtonFoldClick:(UIButton *)button {
- (void)segmented:(UIButton *)button {
    //: _isfold = NO;
    _automatic = NO;
    //: self.buttonsBgView.hidden = YES;
    self.head.hidden = YES;
    //: [self drawPraiseButtons];
    [self attractStopDisk];
}

//: - (void)showViewWithButtonModels:(NSArray *)array
- (void)rect:(NSArray *)array
                 //: cursorStartRect:(CGRect)cursorStartRect selectionTextRectInWindow:(CGRect)rect selectBlock:(void(^)(StylerConfigTexture *item))block praiseBlock:(void(^)(NSInteger tag))praiseblock {
                 cherished:(CGRect)cursorStartRect evidenceBlock:(CGRect)rect response:(void(^)(StylerConfigTexture *item))block draw:(void(^)(NSInteger tag))praiseblock {

    //: _selectBlock = block;
    _proudPresentation = block;
    //: _praiseBlock = praiseblock;
    _joinPaint = praiseblock;

    //    ([UIApplication sharedApplication].delegate).window.backgroundColor = UIColor.redColor;
    //: if (self.superview == nil) {
    if (self.superview == nil) {
        //: [([UIApplication sharedApplication].delegate).window addSubview:self];
        [([UIApplication sharedApplication].delegate).window addSubview:self];
    }
    //: _selectionTextRectInWindow = rect;
    _shear = rect;

    //: UIView *buttonsBgView = [self drawButtonsWithArray:array];
    UIView *buttonsBgView = [self agreement:array];

    //: _bubbleWidth = buttonsBgView.frame.size.width;
    _globuleAspectWidth = buttonsBgView.frame.size.width;
    //: _bubbleHeight = buttonsBgView.frame.size.height + 5;
    _sumactJungle = buttonsBgView.frame.size.height + 5;

    //: CGFloat screenHeight = [UIScreen mainScreen].bounds.size.height;
    CGFloat screenHeight = [UIScreen mainScreen].bounds.size.height;
    //: CGFloat screenWidth = [UIScreen mainScreen].bounds.size.width;
    CGFloat screenWidth = [UIScreen mainScreen].bounds.size.width;

    //: CGRect selfRect = CGRectZero;
    CGRect selfRect = CGRectZero;

    //: CGFloat praiseheight = 0;
    CGFloat praiseheight = 0;
    //: if(_isfold){
    if(_automatic){
        //: praiseheight = 48;
        praiseheight = 48;
    //: }else{
    }else{
        //: praiseheight = 48*5;
        praiseheight = 48*5;
    }

    //默认的方向是优先方向
    //: ImmenseAfterTrustedUrban direction = _directionPriority;
    ImmenseAfterTrustedUrban direction = _sourceTiming;
//
//    if (_directionPriority == EtiqAuBubbleMenuViewDirectionPriorityType_Down) {
//        //Y轴处理，bubble在下面。
//        if (screenHeight - rect.origin.y - rect.size.height - _bubbleContentMargin - _bubbleBottomMargin > _bubbleHeight) {
//            selfRect.origin.y = rect.origin.y + rect.size.height + _bubbleContentMargin;
//            direction = EtiqAuBubbleMenuViewDirectionPriorityType_Down;
//
//            //bubble在上面
//        } else if (rect.origin.y - _bubbleContentMargin - _bubbleBottomMargin > _bubbleHeight) {
//            selfRect.origin.y = rect.origin.y - _bubbleContentMargin - _bubbleHeight;
//            direction = EtiqAuBubbleMenuViewDirectionPriorityType_Up;
//        }
//    } else {
//        //优先级Up的话，先判断在上面的情况。

        //在上面的情况。
        //: if (rect.origin.y - _bubbleContentMargin - _bubbleBottomMargin - praiseheight > _bubbleHeight) {
        if (rect.origin.y - _beneathFloat - _pin - praiseheight > _sumactJungle) {
            //: selfRect.origin.y = rect.origin.y - _bubbleContentMargin - _bubbleHeight - praiseheight + 15;
            selfRect.origin.y = rect.origin.y - _beneathFloat - _sumactJungle - praiseheight + 15;
            //: direction = ImmenseAfterTrustedUrban_Up;
            direction = ImmenseAfterTrustedUrban_Up;

            //在下面的情况。
        //: } else if (screenHeight - rect.origin.y - rect.size.height - _bubbleContentMargin - _bubbleBottomMargin - praiseheight > _bubbleHeight) {
        } else if (screenHeight - rect.origin.y - rect.size.height - _beneathFloat - _pin - praiseheight > _sumactJungle) {
            //: selfRect.origin.y = rect.origin.y + rect.size.height + _bubbleContentMargin -15;
            selfRect.origin.y = rect.origin.y + rect.size.height + _beneathFloat -15;
            //: direction = ImmenseAfterTrustedUrban_Down;
            direction = ImmenseAfterTrustedUrban_Down;

            //上下空间太小，只能压着内容显示该bubbleMenu
        //: } else {
        } else {
            //: selfRect.origin.y = screenHeight - _bubbleBottomMargin - _bubbleHeight - praiseheight -15;
            selfRect.origin.y = screenHeight - _pin - _sumactJungle - praiseheight -15;
            //: direction = ImmenseAfterTrustedUrban_Down;
            direction = ImmenseAfterTrustedUrban_Down;
        }
//    }

    //将所有按钮页面添加到self上面。
    //: CGRect bgViewRect = buttonsBgView.frame;
    CGRect bgViewRect = buttonsBgView.frame;
    //: bgViewRect.origin.y = direction == ImmenseAfterTrustedUrban_Down ? _arrowHeight : praiseheight;
    bgViewRect.origin.y = direction == ImmenseAfterTrustedUrban_Down ? _technology : praiseheight;
    //: buttonsBgView.frame = bgViewRect;
    buttonsBgView.frame = bgViewRect;
    //: [self addSubview:buttonsBgView];
    [self addSubview:buttonsBgView];
//    buttonsBgView.centerX = _praiseWidth/2;


    //X轴处理
//    if (direction == EtiqAuBubbleMenuViewDirectionPriorityType_Down) {
//        selfRect.origin.x = rect.size.width / 2.0 + rect.origin.x - _bubbleWidth / 2.0;
//    } else {
//        //        if ((NSInteger)cursorStartRect.size.height == (NSInteger)rect.size.height) {//同一行
//        //            selfRect.origin.x = rect.size.width / 2.0 + rect.origin.x - _bubbleWidth / 2.0;
//        //        } else {
//        //不在同一行
//        //            selfRect.origin.x = (rect.origin.x + rect.size.width - cursorStartRect.origin.x) / 2.0 + cursorStartRect.origin.x - _bubbleWidth / 2.0;
//        selfRect.origin.x = screenWidth - _praiseWidth;
//        //        }
//    }
    //: if (selfRect.origin.x < _bubbleLeftMargin) {
    if (selfRect.origin.x < _identity) {
        //: selfRect.origin.x = _bubbleLeftMargin;
        selfRect.origin.x = _identity;
    //: } else if (selfRect.origin.x + _bubbleWidth + _bubbleLeftMargin > screenWidth) {
    } else if (selfRect.origin.x + _globuleAspectWidth + _identity > screenWidth) {
        //        selfRect.origin.x = screenWidth - _bubbleWidth - _bubbleLeftMargin;
        //: selfRect.origin.x = screenWidth - _praiseWidth;
        selfRect.origin.x = screenWidth - _redFloat;
    //: }else{
    }else{
        //: selfRect.origin.x = screenWidth - _praiseWidth;
        selfRect.origin.x = screenWidth - _redFloat;
    }

    //: selfRect.size.width = _praiseWidth;
    selfRect.size.width = _redFloat;
    //    selfRect.size.width = _bubbleWidth;
    //: selfRect.size.height = _bubbleHeight+praiseheight;
    selfRect.size.height = _sumactJungle+praiseheight;
    //: self.frame = selfRect;
    self.frame = selfRect;

    // 布局arrow的位置。
    //: CGRect arrowRect = _arrowView.frame;
    CGRect arrowRect = _hematocrit.frame;
    //    CGRect praiseRect = _praiseView.frame;

    //: if (direction == ImmenseAfterTrustedUrban_Up) {
    if (direction == ImmenseAfterTrustedUrban_Up) {
        //: arrowRect.origin.y = selfRect.size.height - arrowRect.size.height;
        arrowRect.origin.y = selfRect.size.height - arrowRect.size.height;
        //: if ((NSInteger)cursorStartRect.size.height == (NSInteger)rect.size.height) {
        if ((NSInteger)cursorStartRect.size.height == (NSInteger)rect.size.height) {
            //: arrowRect.origin.x = rect.origin.x + rect.size.width / 2.0 - selfRect.origin.x - _arrowWidth / 2.0;
            arrowRect.origin.x = rect.origin.x + rect.size.width / 2.0 - selfRect.origin.x - _provisionWidth / 2.0;
        //: } else {
        } else {
            // 如果不是在同一行，需要按照光标位置计算箭头位置。
            //: arrowRect.origin.x = (rect.origin.x + rect.size.width - cursorStartRect.origin.x) / 2.0 + cursorStartRect.origin.x - _arrowWidth / 2.0 - selfRect.origin.x;
            arrowRect.origin.x = (rect.origin.x + rect.size.width - cursorStartRect.origin.x) / 2.0 + cursorStartRect.origin.x - _provisionWidth / 2.0 - selfRect.origin.x;
        }
        //判断如果超出menuView的最右边，让其等于最右边。5是cornerRadius
        //: if (arrowRect.origin.x > selfRect.size.width - 5 - _arrowWidth) {
        if (arrowRect.origin.x > selfRect.size.width - 5 - _provisionWidth) {
            //: arrowRect.origin.x = selfRect.size.width - 5 - _arrowWidth;
            arrowRect.origin.x = selfRect.size.width - 5 - _provisionWidth;
        }
        //: _arrowView.image = [UIImage imageNamed:@"arrowDown"];
        _hematocrit.image = [UIImage imageNamed:StringFromAsterData(commonPossibleTimer)];

        //: _praiseView.bottom = _buttonsBgView.top;
        _drawing.distinct = _head.civil;
        //: _praiseView.left = 0;
        _drawing.empty = 0;
        //        _praiseView.left = rect.size.width/2.0 + rect.origin.x - _praiseView.width/2.0;
    //: } else {
    } else {
        //: arrowRect.origin.x = rect.origin.x + rect.size.width / 2.0 - selfRect.origin.x - _arrowWidth / 2.0;
        arrowRect.origin.x = rect.origin.x + rect.size.width / 2.0 - selfRect.origin.x - _provisionWidth / 2.0;
        //: arrowRect.origin.y = 0;
        arrowRect.origin.y = 0;
        //: _arrowView.image = [UIImage imageNamed:@"arrowUp"];
        _hematocrit.image = [UIImage imageNamed:StringFromAsterData(spacingAbaseHelper)];

        //: _praiseView.top = _buttonsBgView.bottom;
        _drawing.civil = _head.distinct;
        //: _praiseView.left = 0;
        _drawing.empty = 0;
    }


    //: _arrowView.frame = arrowRect;
    _hematocrit.frame = arrowRect;


    //: buttonsBgView.left = arrowRect.origin.x-50;
    buttonsBgView.empty = arrowRect.origin.x-50;

}

//: - (void)clickView{
- (void)forefrontOperator{
    //: self.praiseView.hidden = YES;
    self.drawing.hidden = YES;
    //: self.buttonsBgView.hidden = NO;
    self.head.hidden = NO;
    //: [self removeFromSuperview];
    [self removeFromSuperview];
}

//: - (void)drawPraiseButtons{
- (void)attractStopDisk{

    //: NSArray *viewsArray = _praiseView.subviews;
    NSArray *viewsArray = _drawing.subviews;
    //: for (int i = 0; i < viewsArray.count; i ++) {
    for (int i = 0; i < viewsArray.count; i ++) {
        //: UIView *subview = viewsArray[i];
        UIView *subview = viewsArray[i];
        //: [subview removeFromSuperview];
        [subview removeFromSuperview];
    }


    //: CGFloat topMargin = 10;
    CGFloat topMargin = 10;//button距离上、下面的间距
    //: CGFloat rightMargin = 10;
    CGFloat rightMargin = 10;//button距离左、右面的间距
    //: CGFloat buttonWidth = 28;
    CGFloat buttonWidth = 28;
    //: CGFloat buttonHeight = 28;
    CGFloat buttonHeight = 28;

    //: NSInteger countInOneLine = 8;
    NSInteger countInOneLine = 8;
    //: NSInteger lines = (_buttonCount <= countInOneLine) ? 1:(_buttonCount / countInOneLine);
    NSInteger lines = (_exitQuantity <= countInOneLine) ? 1:(_exitQuantity / countInOneLine);

    //: CGFloat width = rightMargin*(countInOneLine+1) + buttonWidth*countInOneLine;
    CGFloat width = rightMargin*(countInOneLine+1) + buttonWidth*countInOneLine;

    //: if(_isfold){
    if(_automatic){
        //: _praiseView.frame = CGRectMake(0, 0, width, 48);
        _drawing.frame = CGRectMake(0, 0, width, 48);
    //: }else{
    }else{
        //: _praiseView.frame = CGRectMake(0, 0, width, 48*lines);
        _drawing.frame = CGRectMake(0, 0, width, 48*lines);
    }

    //布局所有的按钮
    //: for (int i = 0; i < _buttonCount; i ++) {
    for (int i = 0; i < _exitQuantity; i ++) {

        //: CGFloat x = rightMargin + (i%countInOneLine)*(buttonWidth+rightMargin);
        CGFloat x = rightMargin + (i%countInOneLine)*(buttonWidth+rightMargin);
        //: CGFloat y = topMargin + i/countInOneLine * (buttonHeight + topMargin * 2);
        CGFloat y = topMargin + i/countInOneLine * (buttonHeight + topMargin * 2);

        //: if(_isfold && i==7){
        if(_automatic && i==7){
            //: _foldbtn = [[UIButton alloc] initWithFrame:CGRectMake(x, y, buttonWidth, buttonHeight)];
            _agnize = [[UIButton alloc] initWithFrame:CGRectMake(x, y, buttonWidth, buttonHeight)];
            //: [_foldbtn addTarget:self action:@selector(onButtonFoldClick:) forControlEvents:UIControlEventTouchUpInside];
            [_agnize addTarget:self action:@selector(segmented:) forControlEvents:UIControlEventTouchUpInside];
            //: [_foldbtn setImage:[UIImage imageNamed:@"ic_fold"] forState:UIControlStateNormal];
            [_agnize setImage:[UIImage imageNamed:StringFromAsterData(colorOccasionalLogger)] forState:UIControlStateNormal];
            //: [_praiseView addSubview:_foldbtn];
            [_drawing addSubview:_agnize];

            //: return;
            return;
        }

        //: UIButton *button = [[UIButton alloc] initWithFrame:CGRectMake(x, y, buttonWidth, buttonHeight)];
        UIButton *button = [[UIButton alloc] initWithFrame:CGRectMake(x, y, buttonWidth, buttonHeight)];
        //: button.tag = 1 + i;
        button.tag = 1 + i;
        //: [button addTarget:self action:@selector(onButtonClick:) forControlEvents:UIControlEventTouchUpInside];
        [button addTarget:self action:@selector(finishDirty:) forControlEvents:UIControlEventTouchUpInside];
        //: [button setImage:[UIImage imageNamed:[NSString stringWithFormat:@"ic_praise%d",i+1]] forState:UIControlStateNormal];
        [button setImage:[UIImage imageNamed:[NSString stringWithFormat:StringFromAsterData(componentThingQueryAlert),i+1]] forState:UIControlStateNormal];
        //        button.backgroundColor = RGB_COLOR_String(@"#4355B5");
        //: [_praiseView addSubview:button];
        [_drawing addSubview:button];


    }


}

//: - (id)initWithFrame:(CGRect)frame {
- (id)initWithFrame:(CGRect)frame {
    //: self = [super initWithFrame:frame];
    self = [super initWithFrame:frame];
    //: if (self) {
    if (self) {
        //初始化默认数据
        //: self.backgroundColor = UIColor.clearColor;
        self.backgroundColor = UIColor.clearColor;
        //: _isfold = YES;
        _automatic = YES;
         //: _buttonCount = 40;
         _exitQuantity = 40;
        //: _praiseWidth = 314;
        _redFloat = 314;
        //: _arrowHeight = 20;
        _technology = 20;
        //: _bubbleLeftMargin = 20;
        _identity = 20;
        //: _bubbleContentMargin = 15;
        _beneathFloat = 15;
        //: _bubbleBottomMargin = 20;
        _pin = 20;
        //: _directionPriority = ImmenseAfterTrustedUrban_Up;
        _sourceTiming = ImmenseAfterTrustedUrban_Up;

        //: UITapGestureRecognizer *singleTapbox1 = [[UITapGestureRecognizer alloc]initWithTarget:self action:@selector(clickView)];
        UITapGestureRecognizer *singleTapbox1 = [[UITapGestureRecognizer alloc]initWithTarget:self action:@selector(forefrontOperator)];
        //: [self addGestureRecognizer:singleTapbox1];
        [self addGestureRecognizer:singleTapbox1];


        //: _buttonsBgView = [[UIView alloc] init];
        _head = [[UIView alloc] init];
        //: _buttonsBgView.backgroundColor = [UIColor whiteColor];
        _head.backgroundColor = [UIColor whiteColor];
        //: _buttonsBgView.layer.cornerRadius = 8;
        _head.layer.cornerRadius = 8;
        //: _buttonsBgView.clipsToBounds = true;
        _head.clipsToBounds = true;
        //: [self addSubview:_buttonsBgView];
        [self addSubview:_head];

//        _arrowWidth = 11;
//        _arrowHeight = 5;
//        _arrowView = [[UIImageView alloc] initWithFrame:CGRectMake(0, 0, _arrowWidth, _arrowHeight)];
////        _arrowView.backgroundColor = UIColor.greenColor;
//        [self addSubview:_arrowView];

        //: _praiseView = [[UIView alloc]initWithFrame:CGRectMake(0, 0, _praiseWidth, 48)];
        _drawing = [[UIView alloc]initWithFrame:CGRectMake(0, 0, _redFloat, 48)];
        //: _praiseView.backgroundColor = [UIColor colorWithWhite:0 alpha:0.8];
        _drawing.backgroundColor = [UIColor colorWithWhite:0 alpha:0.8];
        //: _praiseView.layer.borderWidth = 1;
        _drawing.layer.borderWidth = 1;
        //: _praiseView.layer.borderColor = [UIColor colorWithRed:255/255.0 green:255/255.0 blue:255/255.0 alpha:0.2].CGColor;
        _drawing.layer.borderColor = [UIColor colorWithRed:255/255.0 green:255/255.0 blue:255/255.0 alpha:0.2].CGColor;
        //: _praiseView.layer.cornerRadius = 12;
        _drawing.layer.cornerRadius = 12;
        //: [self addSubview:_praiseView];
        [self addSubview:_drawing];
        //: _praiseView.hidden = YES;
        _drawing.hidden = YES;
        //: [self drawPraiseButtons];
        [self attractStopDisk];

        //: _oldContentArray = [[NSMutableArray alloc] init];
        _extended = [[NSMutableArray alloc] init];
    }
    //: return self;
    return self;
}

//: - (void)onButtonClick:(UIButton *)button {
- (void)finishDirty:(UIButton *)button {

    //: self.praiseView.hidden = YES;
    self.drawing.hidden = YES;
    //: self.buttonsBgView.hidden = NO;
    self.head.hidden = NO;

    //: if (self.praiseBlock != nil) {
    if (self.joinPaint != nil) {
        //: self.praiseBlock(button.tag);
        self.joinPaint(button.tag);
    }

}

//: + (instancetype)shareMenuView {
+ (instancetype)minuteResponse {
    //: static ToMediatorShore *menu = nil;
    static ToMediatorShore *menu = nil;
    //: static dispatch_once_t onceToken;
    static dispatch_once_t onceToken;
    //: _dispatch_once(&onceToken, ^{
    _dispatch_once(&onceToken, ^{
        //: if (menu == nil) {
        if (menu == nil) {
            //: menu = [[ToMediatorShore alloc] init];
            menu = [[ToMediatorShore alloc] init];
        }
    //: });
    });
  //: return menu;
  return menu;
}

//判断button的信息内容是不是修改了，true：改动了，   false：内容没有变
//: - (BOOL)ifButtonsInfoChanged:(NSArray *)newArray {
- (BOOL)signiorChanged:(NSArray *)newArray {
    //: if (_oldContentArray.count != newArray.count) {
    if (_extended.count != newArray.count) {
        //: _oldContentArray = [NSMutableArray arrayWithArray:newArray];
        _extended = [NSMutableArray arrayWithArray:newArray];
        //: return true;
        return true;
    }

    //: for (int i = 0; i < newArray.count; i ++) {
    for (int i = 0; i < newArray.count; i ++) {
        //: BOOL finded = false;
        BOOL finded = false;
        //: NSInteger newId = ((MarkTransformerConductorOutline *)_oldContentArray[i]).buttonId;
        NSInteger newId = ((MarkTransformerConductorOutline *)_extended[i]).signer;
        //: for (int j = 0; j < _oldContentArray.count; j ++) {
        for (int j = 0; j < _extended.count; j ++) {
            //: NSInteger oldId = ((MarkTransformerConductorOutline *)_oldContentArray[j]).buttonId;
            NSInteger oldId = ((MarkTransformerConductorOutline *)_extended[j]).signer;
            //: if (newId == oldId) {
            if (newId == oldId) {
                //: finded = true;
                finded = true;
                //: continue;
                continue;
            }
        }
        // 如果没有找到
        //: if (finded != true) {
        if (finded != true) {
            //: _oldContentArray = [NSMutableArray arrayWithArray:newArray];
            _extended = [NSMutableArray arrayWithArray:newArray];
            //: return true;
            return true;
        }
    }
    //: return false;
    return false;
}

//: - (void)onButtonTouched:(UIButton *)button {
- (void)wealthying:(UIButton *)button {
    //: if (_myNewContentArray.count > button.tag - 100) {
    if (_array.count > button.tag - 100) {
        //: MarkTransformerConductorOutline *model = (MarkTransformerConductorOutline *)_myNewContentArray[button.tag - 100];
        MarkTransformerConductorOutline *model = (MarkTransformerConductorOutline *)_array[button.tag - 100];


        //: if([model.name isEqual:[PluginTulipOptimize getTextWithKey:@"friend_circle_adapter_like"]]){
        if([model.linkText isEqual:[PluginTulipOptimize richness:StringFromAsterData(kWallMessage)]]){
            //: self.praiseView.hidden = NO;
            self.drawing.hidden = NO;
        //: }else{
        }else{
            //: self.praiseView.hidden = YES;
            self.drawing.hidden = YES;

            //: if (self.selectBlock != nil) {
            if (self.proudPresentation != nil) {
                //: self.selectBlock(model.item);
                self.proudPresentation(model.mechanically);
            }
        }
    }
}

//返回整个buttons所占的view的宽高。返回之后，需加上箭头的高度，就是这个self的宽高。
//: - (UIView *)drawButtonsWithArray:(NSArray *)array {
- (UIView *)agreement:(NSArray *)array {

    //如果数据发生了变化,将所有buttons都删掉，重新画，如果没有变化，不需要重新画，直接返回。
    //    if ([self ifButtonsInfoChanged:array]) {
    //: NSArray *viewsArray = _buttonsBgView.subviews;
    NSArray *viewsArray = _head.subviews;
    //: for (int i = 0; i < viewsArray.count; i ++) {
    for (int i = 0; i < viewsArray.count; i ++) {
        //: UIView *subview = viewsArray[i];
        UIView *subview = viewsArray[i];
        //: [subview removeFromSuperview];
        [subview removeFromSuperview];
    }
    //    } else {
    //        return _buttonsBgView;
    //    }
    //: _myNewContentArray = array;
    _array = array;

    //: CGFloat topMargin = 12;
    CGFloat topMargin = 12;//button距离上、下面的间距
    //: CGFloat rightMargin = 13;
    CGFloat rightMargin = 13;//button距离左、右面的间距
    //: CGFloat buttonWidth = 85;
    CGFloat buttonWidth = 85;
    //: CGFloat buttonHeight = 30;
    CGFloat buttonHeight = 30;

    //: NSInteger countInOneLine = 5;
    NSInteger countInOneLine = 5;

    //    NSInteger lines = (array.count <= countInOneLine) ? 1:(array.count / countInOneLine + 1);

    //确定按钮背景的视图尺寸。
    //    CGFloat width = rightMargin * 2 + ((array.count > countInOneLine)? countInOneLine : array.count) * buttonWidth;
    //    CGFloat height = lines * (2 * topMargin + buttonHeight);
    //: CGFloat width = rightMargin * 2 + buttonWidth;
    CGFloat width = rightMargin * 2 + buttonWidth;
    //: CGFloat height = topMargin * 2 + array.count * buttonHeight;
    CGFloat height = topMargin * 2 + array.count * buttonHeight;
    //: _buttonsBgView.frame = CGRectMake(0, 0, width, height);
    _head.frame = CGRectMake(0, 0, width, height);

    //布局所有的按钮
    //: for (int i = 0; i < array.count; i ++) {
    for (int i = 0; i < array.count; i ++) {
        //        CGFloat x = rightMargin + i % countInOneLine * buttonWidth;
        //        CGFloat y = topMargin + i / countInOneLine * (buttonHeight + topMargin * 2);
        //: CGFloat x = rightMargin;
        CGFloat x = rightMargin;
        //: CGFloat y = topMargin + i*buttonHeight;
        CGFloat y = topMargin + i*buttonHeight;
        //: UIButton *button = [[UIButton alloc] initWithFrame:CGRectMake(x, y, buttonWidth, buttonHeight)];
        UIButton *button = [[UIButton alloc] initWithFrame:CGRectMake(x, y, buttonWidth, buttonHeight)];
        //: button.tag = 100 + i;
        button.tag = 100 + i;
        //: [button addTarget:self action:@selector(onButtonTouched:) forControlEvents:UIControlEventTouchUpInside];
        [button addTarget:self action:@selector(wealthying:) forControlEvents:UIControlEventTouchUpInside];

        //: MarkTransformerConductorOutline *model = array[i];
        MarkTransformerConductorOutline *model = array[i];

        //: UIImageView *imageView = [[UIImageView alloc] initWithFrame:CGRectMake(0, 7, 16, 16)];
        UIImageView *imageView = [[UIImageView alloc] initWithFrame:CGRectMake(0, 7, 16, 16)];
        //: if (model.normalImage) {
        if (model.moralReflexion) {
            //: imageView.image = model.normalImage;
            imageView.image = model.moralReflexion;
        //: } else {
        } else {
            //: imageView.image = [UIImage imageNamed:model.imageName];
            imageView.image = [UIImage imageNamed:model.precious];
        }
        //: imageView.contentMode = UIViewContentModeScaleAspectFit;
        imageView.contentMode = UIViewContentModeScaleAspectFit;
        //: [button addSubview:imageView];
        [button addSubview:imageView];

        //: UILabel *label = [[UILabel alloc] initWithFrame:CGRectMake(25, 0, buttonWidth-25, 30)];
        UILabel *label = [[UILabel alloc] initWithFrame:CGRectMake(25, 0, buttonWidth-25, 30)];
        //: label.font = [UIFont systemFontOfSize:13];
        label.font = [UIFont systemFontOfSize:13];
        //        label.textAlignment = NSTextAlignmentCenter;
        //: label.textColor = [UIColor colorWithHexString:@"#333333"];
        label.textColor = [UIColor tin:StringFromAsterData(componentAppealShooSettings)];
        //: label.text = model.name;
        label.text = model.linkText;
        //: [button addSubview:label];
        [button addSubview:label];

        //        button.backgroundColor = i % 2 == 0 ? UIColor.redColor : UIColor.greenColor;
        //: [_buttonsBgView addSubview:button];
        [_head addSubview:button];
    }

    //: _buttonsBgView.backgroundColor = [UIColor colorWithHexString:@"FFFCF7"];
    _head.backgroundColor = [UIColor tin:StringFromAsterData(appLeadingFormat)];

    //: return _buttonsBgView;
    return _head;
}

//: @end
@end

Byte * AsterDataToCache(Byte *data) {
    int except = data[0];
    int playerShock = data[1];
    Byte technicianBear = data[2];
    int roperCove = data[3];
    if (!except) return data + roperCove;
    for (int i = roperCove; i < roperCove + playerShock; i++) {
        int value = data[i] + technicianBear;
        if (value > 255) {
            value -= 256;
        }
        data[i] = value;
    }
    data[0] = 0;
    data[roperCove + playerShock] = 0;
    return data + roperCove;
}

NSString *StringFromAsterData(Byte *data) {
    return [NSString stringWithUTF8String:(char *)AsterDataToCache(data)];
}
